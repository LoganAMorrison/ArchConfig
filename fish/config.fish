# This will make pyenv python versions come before system python
# set PATH $HOME/.pyenv/shims $PATH

# Clion path
set PATH $HOME/Applications/clion-2020.2/bin $PATH
# Add Ruby to path
set PATH $HOME/.gem/ruby/2.7.0/bin $PATH
# Add .local to path
set PATH $HOME/.local/bin $HOME/.local/lib $PATH

starship init fish | source
